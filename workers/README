Requirements:  django, python-mysqldb, django-celery and celery installed. 

Run ./exec_worker.sh worker_name to start a worker

./exec_worker.sh:
    Provides an easy way to execute multiple workers from within the same directory.
    This will automatically create a worker which imports from the base_worker. 
    
